apiVersion: v1
kind: Service
metadata:
  name: app-service
spec:
  selector:
    app: pmironenko-app
  ports:
    - name: web
      port: 8000
      targetPort: 8000
    - name: db
      port: 5432
      targetPort: 5432
  type: ClusterIP
